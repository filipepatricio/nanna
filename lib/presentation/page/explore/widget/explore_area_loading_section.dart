import 'package:flutter/material.dart';

class ExploreAreaLoadingSection extends StatelessWidget {
  const ExploreAreaLoadingSection({Key? key}) : super(key: key);

  const ExploreAreaLoadingSection.large({Key? key}) : super(key: key);

  const ExploreAreaLoadingSection.small({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
