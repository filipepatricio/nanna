# import 'package:better_informed_mobile/data/gql/common/bookmark_fragment.graphql';

mutation bookmarkArticle($slug: String!){
    bookmarkArticle(slug: $slug) {
        ...bookmarkResponseFragment
    }
}