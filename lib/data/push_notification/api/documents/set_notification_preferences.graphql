mutation setNotificationPreferences($id: String!, $pushEnabled: Boolean, $emailEnabled: Boolean){
    setNotificationChannelPreferences(id: $id, pushEnabled: $pushEnabled, emailEnabled: $emailEnabled) {
        id
        name
        description
        pushEnabled
        emailEnabled
    }
}
