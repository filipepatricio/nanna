query getNotificationPreferencesMobile{
    getNotificationPreferences {
        name
        channels {
            id
            name
            pushEnabled
            emailEnabled
        }
    }
}